# 📚 دليل المنصة الشامل - نظام إدارة التعلم (LMS)

## 🎯 فكرة المشروع

منصة تعليمية إلكترونية شاملة باللغة العربية مصممة لطلاب المراحل الإعدادية والثانوية (الصفوف 7-12). تتيح المنصة للطلاب التسجيل في الدورات التعليمية، مشاهدة الفيديوهات، أداء الاختبارات، ومتابعة التقدم الدراسي. كما تتيح لأولياء الأمور متابعة أداء أبنائهم.

---

## 🏗️ البنية التقنية

### التقنيات المستخدمة:
- **Frontend**: React.js مع React Router
- **Backend**: Node.js + Express.js
- **Database**: MongoDB (MongoDB Atlas)
- **Authentication**: JWT (JSON Web Tokens)
- **File Storage**: GridFS + Multer
- **Real-time**: Socket.io
- **Payment**: PayPal, Stripe, Vodafone Cash, Bank Transfer

### هيكل المشروع:

```
├── client/              # تطبيق React Frontend
│   ├── src/
│   │   ├── components/  # المكونات القابلة لإعادة الاستخدام
│   │   ├── pages/       # صفحات التطبيق
│   │   ├── contexts/    # Context API للدولة العامة
│   │   ├── services/    # خدمات API
│   │   └── store/       # Redux Store
│   └── public/          # الملفات الثابتة
│
├── server/              # تطبيق Express Backend
│   ├── models/          # نماذج MongoDB
│   ├── controllers/     # معالجات الطلبات
│   ├── routes/          # مسارات API
│   ├── middleware/      # Middleware مخصص
│   ├── utils/           # أدوات مساعدة
│   └── server.js        # نقطة البداية
│
└── deployment/          # ملفات النشر
```

---

## 👥 الأدوار والصلاحيات

### 1️⃣ **الأدمن (Admin)**

#### الصلاحيات:
- ✅ إدارة كاملة للمنصة
- ✅ إنشاء وتعديل وحذف الدورات
- ✅ إدارة المستخدمين (طلاب، أولياء أمور)
- ✅ الموافقة/الرفض على المدفوعات
- ✅ إدارة المجموعات الدراسية
- ✅ إنشاء الاختبارات والامتحانات
- ✅ عرض التقارير والإحصائيات
- ✅ إرسال الإشعارات
- ✅ إدارة إعدادات النظام

#### الوظائف الرئيسية:

**1. إدارة الدورات:**
- إنشاء دورات جديدة مع تحديد:
  - العنوان والوصف
  - الصف الدراسي (7-12)
  - المادة الدراسية
  - الفصل الدراسي (Term 1, Term 2)
  - السعر
  - الصور (Cover Image)
- إضافة فيديوهات (YouTube, Vimeo, Custom)
- إنشاء اختبارات داخلية:
  - أسئلة اختيار من متعدد (MCQ)
  - أسئلة صح/خطأ (True/False)
  - أسئلة مقالية (Essay)
- ربط امتحانات خارجية (Google Forms, External Links)
- تعديل وحذف الدورات

**2. إدارة المستخدمين:**
- عرض جميع المستخدمين مع فلترة:
  - حسب الدور (طالب، ولي أمر، أدمن)
  - حسب الصف الدراسي
  - حسب المحافظة
- إنشاء حسابات جديدة
- تعديل بيانات المستخدمين
- حذف المستخدمين
- تفعيل/تعطيل الحسابات
- الموافقة على طلبات أولياء الأمور

**3. إدارة المدفوعات:**
- عرض جميع طلبات الدفع
- مراجعة إثباتات الدفع (Screenshots)
- الموافقة على المدفوعات:
  - تفعيل اشتراك الطالب في الدورة تلقائياً
  - إرسال إشعار للمستخدم
- رفض المدفوعات مع إضافة سبب الرفض
- عرض إحصائيات المدفوعات

**4. إدارة المجموعات:**
- إنشاء مجموعات دراسية
- إضافة/إزالة طلاب من المجموعات
- نشر إعلانات في المجموعات
- إدارة المحادثات الجماعية
- رفع ملفات ومشاركة روابط

**5. إدارة الاختبارات:**
- إنشاء اختبارات داخلية مع:
  - تحديد عدد الأسئلة
  - تحديد الدرجة الكلية
  - تحديد وقت الامتحان
  - تحديد درجة النجاح
- مراجعة نتائج الطلاب
- تصحيح الأسئلة المقالية يدوياً

**6. التقارير والإحصائيات:**
- إحصائيات عامة:
  - عدد الطلاب المسجلين
  - عدد الدورات
  - إجمالي المدفوعات
  - عدد الاختبارات المكتملة
- تقارير الأداء:
  - أداء الطلاب في الاختبارات
  - أكثر الدورات شعبية
  - معدلات النجاح

**7. نظام الإشعارات:**
- إرسال إشعارات عامة لجميع المستخدمين
- إرسال إشعارات لطلاب محددين
- إشعارات تلقائية عند:
  - الموافقة على الدفع
  - إضافة فيديو جديد
  - إضافة اختبار جديد

#### المسارات (Routes):
- `/admin` - لوحة التحكم الرئيسية
- `/admin/courses` - إدارة الدورات
- `/admin/students` - إدارة الطلاب
- `/admin/payments` - إدارة المدفوعات
- `/admin/groups` - إدارة المجموعات
- `/admin/messages` - نظام الرسائل
- `/admin/analytics` - التقارير والإحصائيات
- `/admin/settings` - إعدادات النظام

---

### 2️⃣ **الطالب (Student)**

#### الصلاحيات:
- ✅ التسجيل وإنشاء حساب
- ✅ تصفح الدورات المتاحة
- ✅ التسجيل في الدورات (بعد الدفع)
- ✅ مشاهدة الفيديوهات
- ✅ أداء الاختبارات
- ✅ متابعة التقدم الدراسي
- ✅ عرض الإحصائيات الشخصية
- ✅ الانضمام للمجموعات الدراسية
- ✅ رفع إثباتات الدفع

#### الوظائف الرئيسية:

**1. التسجيل والدخول:**
- التسجيل باستخدام:
  - الاسم الكامل (4 أسماء)
  - رقم الهاتف (E.164 format)
  - كلمة المرور
  - الصف الدراسي
  - المحافظة
- تسجيل الدخول برقم الهاتف وكلمة المرور
- استعادة كلمة المرور

**2. تصفح الدورات:**
- عرض جميع الدورات المتاحة
- فلترة الدورات حسب:
  - الصف الدراسي
  - المادة الدراسية
  - الفصل الدراسي
- عرض تفاصيل الدورة:
  - الوصف
  - السعر
  - المحتوى (فيديوهات، اختبارات)
  - عدد الطلاب المسجلين

**3. التسجيل في الدورات:**
- اختيار دورة
- رفع إثبات الدفع (Screenshot)
- انتظار موافقة الأدمن
- تفعيل الدورة تلقائياً بعد الموافقة

**4. مشاهدة المحتوى:**
- عرض قائمة الفيديوهات في الدورة
- تشغيل الفيديوهات (YouTube, Vimeo, Custom)
- تتبع التقدم:
  - الفيديوهات المشاهدة
  - نسبة الإنجاز
- إضافة فيديوهات للمفضلة

**5. أداء الاختبارات:**
- عرض الاختبارات المتاحة في الدورة
- بدء الاختبار:
  - عرض الأسئلة
  - اختيار الإجابات
  - مؤقت زمني (إن وجد)
- إرسال الإجابات
- عرض النتيجة فوراً:
  - الدرجة الكلية
  - النسبة المئوية
  - التقدير (A+, A, B+, إلخ)
  - الإجابات الصحيحة/الخاطئة

**6. متابعة التقدم:**
- لوحة معلومات شخصية:
  - الدورات المسجل فيها
  - نسبة الإنجاز في كل دورة
  - آخر الاختبارات
  - الإحصائيات العامة
- إحصائيات مفصلة:
  - أداء الاختبارات
  - الوقت المستغرق في الدراسة
  - التقدم الأسبوعي/الشهري

**7. المجموعات الدراسية:**
- عرض المجموعات المنضم إليها
- قراءة الإعلانات
- المشاركة في المحادثات (إن كان مسموحاً)
- عرض الملفات المشتركة

**8. الملف الشخصي:**
- عرض وتعديل البيانات الشخصية
- تغيير كلمة المرور
- عرض تاريخ المدفوعات
- عرض الإشعارات

#### المسارات (Routes):
- `/` - الصفحة الرئيسية
- `/courses` - تصفح الدورات
- `/courses/:id` - تفاصيل الدورة
- `/courses/:id/content` - محتوى الدورة
- `/courses/:id/exam/:examId` - أداء الاختبار
- `/student/stats` - الإحصائيات الشخصية
- `/my-subscriptions` - اشتراكاتي
- `/my-groups` - مجموعاتي
- `/profile` - الملف الشخصي

---

### 3️⃣ **ولي الأمر (Parent)**

#### الصلاحيات:
- ✅ التسجيل وإنشاء حساب
- ✅ ربط حساب الطالب بحساب ولي الأمر
- ✅ متابعة أداء الأبناء
- ✅ عرض نتائج الاختبارات
- ✅ عرض التقدم الدراسي
- ✅ التواصل مع الإدارة

#### الوظائف الرئيسية:

**1. التسجيل:**
- التسجيل كولي أمر
- إدخال بيانات الاتصال
- تحديد صلة القرابة (أب، أم، ولي أمر)
- انتظار موافقة الأدمن

**2. ربط حساب الطالب:**
- البحث عن الطالب:
  - برقم الهاتف
  - برقم الطالب (Student ID)
  - بالاسم
- طلب الربط
- انتظار الموافقة من الأدمن

**3. متابعة الأداء:**
- عرض قائمة الأبناء المرتبطين
- اختيار طالب لمتابعة أدائه
- عرض لوحة معلومات شاملة:
  - الدورات المسجل فيها
  - نسبة الإنجاز
  - آخر الاختبارات
  - الدرجات والتقديرات

**4. عرض النتائج:**
- عرض جميع نتائج الاختبارات
- تفاصيل كل اختبار:
  - الدرجة
  - النسبة المئوية
  - التقدير
  - الإجابات الصحيحة/الخاطئة
- رسوم بيانية للأداء:
  - التقدم الزمني
  - توزيع الدرجات
  - المقارنة بين المواد

**5. الإشعارات:**
- إشعارات تلقائية عند:
  - إكمال الطالب لاختبار
  - إضافة دورة جديدة
  - تحديثات مهمة

#### المسارات (Routes):
- `/parent/login` - تسجيل الدخول
- `/parent/dashboard` - لوحة التحكم
- `/parent/link-student` - ربط حساب طالب
- `/parent/student/:studentId/stats` - إحصائيات طالب

---

## 📝 نظام الامتحانات - شرح شامل

### 🎯 نظرة عامة

نظام الامتحانات في المنصة يدعم ثلاثة أنواع من الاختبارات:
1. **الاختبارات الداخلية** - يتم إنشاؤها وإدارتها داخل المنصة
2. **Google Forms** - روابط خارجية لاختبارات Google
3. **External Links** - روابط خارجية مخصصة

---

### 1️⃣ إنشاء الامتحان عند الأدمن

#### الخطوات التفصيلية:

**أ) الوصول إلى صفحة إنشاء/تعديل الدورة:**
- الأدمن يذهب إلى `/admin/courses`
- يضغط على "إنشاء دورة جديدة" أو "تعديل دورة موجودة"
- في صفحة الدورة، يوجد قسم خاص للامتحانات

**ب) إضافة امتحان جديد:**

1. **معلومات الامتحان الأساسية:**
   - العنوان: عنوان الامتحان (مطلوب)
   - النوع: `internal_exam` / `google_form` / `external` / `link`
   - المدة الزمنية: بالدقائق (اختياري، 0 للاختبارات الخارجية)
   - الدرجة الكلية: مجموع درجات جميع الأسئلة
   - درجة النجاح: النسبة المئوية المطلوبة للنجاح (افتراضي: 60%)
   - رابط خارجي: (للاختبارات الخارجية فقط)

2. **إضافة الأسئلة (للاختبارات الداخلية فقط):**

   **نوع السؤال: اختيار من متعدد (MCQ/Multiple Choice)**
   
   خطوات الإضافة:
   1. إدخال نص السؤال
   2. تحديد نوع السؤال: `"multiple_choice"` أو `"mcq"`
   3. إضافة الخيارات (حد أدنى خيارين):
      - كل خيار له معرف فريد (ID) تلقائياً
      - نص الخيار
   4. تحديد الإجابة الصحيحة:
      - ✅ نظام الاختيار الواحد فقط
      - اختيار خيار واحد فقط كإجابة صحيحة
      - يتم حفظ معرف الخيار الصحيح (`option.id`)
   5. تحديد عدد النقاط (افتراضي: 1 نقطة)

   **مثال على سؤال MCQ:**
   ```javascript
   {
     id: "q_1234567890_0",
     questionText: "ما هي عاصمة مصر؟",
     type: "multiple_choice",
     points: 2,
     options: [
       { id: "opt_q_1234567890_0_0", text: "الإسكندرية" },
       { id: "opt_q_1234567890_0_1", text: "القاهرة" },  // ✅ الصحيحة
       { id: "opt_q_1234567890_0_2", text: "الجيزة" },
       { id: "opt_q_1234567890_0_3", text: "أسوان" }
     ],
     correctAnswer: "opt_q_1234567890_0_1"  // ✅ معرف الخيار الصحيح
   }
   ```

   **نوع السؤال: صح/خطأ (True/False)**
   
   خطوات الإضافة:
   1. إدخال نص السؤال
   2. تحديد نوع السؤال: `"true_false"`
   3. تحديد الإجابة الصحيحة:
      - `true` (صحيح)
      - `false` (خطأ)
   4. تحديد عدد النقاط (افتراضي: 1 نقطة)

   **مثال على سؤال True/False:**
   ```javascript
   {
     id: "q_1234567890_1",
     questionText: "مصر تقع في قارة أفريقيا",
     type: "true_false",
     points: 1,
     correctAnswer: true  // ✅ قيمة boolean
   }
   ```

   **نوع السؤال: مقالي (Essay)**
   
   خطوات الإضافة:
   1. إدخال نص السؤال
   2. تحديد نوع السؤال: `"essay"`
   3. إضافة إجابة نموذجية (اختياري):
      - تساعد الأدمن في التصحيح اليدوي
   4. تحديد عدد النقاط (افتراضي: 1 نقطة)

   **مثال على سؤال Essay:**
   ```javascript
   {
     id: "q_1234567890_2",
     questionText: "اشرح أهمية التعليم في المجتمع",
     type: "essay",
     points: 10,
     sampleAnswer: "التعليم هو أساس تقدم المجتمعات..."  // اختياري
   }
   ```

3. **حفظ الامتحان:**
   - بعد إضافة جميع الأسئلة
   - الضغط على "حفظ الامتحان"
   - يتم التحقق من:
     - وجود سؤال واحد على الأقل (للاختبارات الداخلية)
     - جميع الأسئلة لها إجابات صحيحة محددة
     - الخيارات مكتملة (لأسئلة MCQ)

**ج) هيكل البيانات المحفوظة:**

```javascript
// في Course Model
exams: [{
  id: "exam_1234567890",
  title: "امتحان الفصل الأول",
  type: "internal_exam",
  duration: 60,  // دقائق
  totalMarks: 100,
  totalPoints: 100,
  passingScore: 60,  // نسبة مئوية
  questions: [
    // ... الأسئلة كما في الأمثلة أعلاه
  ],
  isActive: true,
  createdAt: Date
}]
```

---

### 2️⃣ أداء الامتحان عند الطالب

#### الخطوات التفصيلية:

**أ) الوصول إلى الامتحان:**
1. الطالب يفتح الدورة المسجل فيها
2. يذهب إلى قسم "الامتحانات"
3. يرى قائمة بجميع الامتحانات المتاحة
4. يضغط على "بدء الامتحان"

**ب) واجهة أداء الامتحان:**

1. **شاشة البداية:**
   - عنوان الامتحان
   - عدد الأسئلة
   - المدة الزمنية (إن وجدت)
   - الدرجة الكلية
   - درجة النجاح
   - زر "بدء الامتحان"

2. **شاشة الأسئلة:**
   - عرض سؤال واحد في كل مرة (أو جميع الأسئلة حسب التصميم)
   - شريط تقدم يوضح:
     * السؤال الحالي / إجمالي الأسئلة
     * الأسئلة المجابة / غير المجابة
   - مؤقت زمني (إن وجد):
     * العد التنازلي
     * تحذير عند اقتراب انتهاء الوقت
   - أزرار التنقل:
     * السابق
     * التالي
     * قائمة الأسئلة (للانتقال المباشر)

3. **أنواع الإجابات:**

   **لأسئلة MCQ:**
   - عرض جميع الخيارات كأزرار أو radio buttons
   - الطالب يختار خيار واحد فقط
   - عند الاختيار، يتم حفظ معرف الخيار (`option.id`)
   - يمكن تغيير الإجابة قبل الإرسال

   **لأسئلة True/False:**
   - زران: "صحيح" و "خطأ"
   - الطالب يختار واحد فقط
   - يتم حفظ القيمة: `true` أو `false`

   **لأسئلة Essay:**
   - حقل نصي كبير
   - الطالب يكتب إجابته
   - لا يوجد حد أدنى أو أقصى للكلمات (يمكن إضافته)

4. **إرسال الامتحان:**
   - زر "إرسال الإجابات"
   - تحذير: "هل أنت متأكد من إرسال الإجابات؟"
   - التحقق من إجابة جميع الأسئلة
   - إرسال البيانات إلى الخادم

**ج) تنسيق البيانات المرسلة:**

```javascript
// البيانات المرسلة من الطالب
{
  courseId: "course_123",
  examId: "exam_1234567890",
  answers: [
    {
      questionId: "q_1234567890_0",
      answer: "opt_q_1234567890_0_1"  // ✅ معرف الخيار للـ MCQ
    },
    {
      questionId: "q_1234567890_1",
      answer: true  // ✅ قيمة boolean للـ True/False
    },
    {
      questionId: "q_1234567890_2",
      answer: "التعليم مهم لأنه..."  // ✅ نص للإجابة المقالية
    }
  ],
  timeSpent: 1800  // الوقت المستغرق بالثواني (اختياري)
}
```

---

### 3️⃣ التصحيح التلقائي - شرح تفصيلي

#### 🎯 نظرة عامة

المنصة تستخدم نظام تصحيح تلقائي فوري للأسئلة الموضوعية (MCQ و True/False). الأسئلة المقالية تحتاج تصحيح يدوي من الأدمن.

#### العملية خطوة بخطوة:

**أ) استقبال الإجابات:**

1. **الخادم يستقبل الطلب:**
   ```
   POST /api/exams/:courseId/:examId/submit
   Body: { answers: [...], timeSpent: 1800 }
   ```

2. **التحقق من الصلاحيات:**
   - ✅ التحقق من تسجيل الطالب في الدورة
   - ✅ التحقق من عدم وجود نتيجة سابقة (إن لم يكن الامتحان قابل للتعديل)
   - ✅ جلب بيانات الامتحان من قاعدة البيانات

**ب) عملية التصحيح:**

1. **تهيئة المتغيرات:**
   ```javascript
   let totalScore = 0;        // الدرجة الإجمالية
   let maxScore = 0;          // الدرجة القصوى
   let correctAnswers = 0;    // عدد الإجابات الصحيحة
   const gradedAnswers = [];  // تفاصيل كل سؤال
   ```

2. **التكرار على كل سؤال:**

   **لأسئلة MCQ (اختيار من متعدد):**
   ```javascript
   // لكل سؤال:
   const question = exam.questions[qIndex];
   const userAnswer = answers.find(a => a.questionId === question.id);
   
   // ✅ المقارنة: معرف الخيار الصحيح === إجابة الطالب
   if (question.type === 'multiple_choice' || question.type === 'mcq') {
     // question.correctAnswer = "opt_q_1234567890_0_1" (string)
     // userAnswer.answer = "opt_q_1234567890_0_1" (string)
     
     if (question.correctAnswer === userAnswer.answer) {
       isCorrect = true;
       pointsEarned = question.points || 1;
     } else {
       isCorrect = false;
       pointsEarned = 0;
     }
   }
   ```

   **لأسئلة True/False:**
   ```javascript
   if (question.type === 'true_false') {
     // question.correctAnswer = true (boolean)
     // userAnswer.answer = true (boolean)
     
     // تحويل القيم إلى boolean للمقارنة
     const correctBool = Boolean(question.correctAnswer);
     const userBool = Boolean(userAnswer.answer === true || 
                              userAnswer.answer === 'true' || 
                              userAnswer.answer === 'صحيح');
     
     if (correctBool === userBool) {
       isCorrect = true;
       pointsEarned = question.points || 1;
     } else {
       isCorrect = false;
       pointsEarned = 0;
     }
   }
   ```

   **لأسئلة Essay (مقالية):**
   ```javascript
   if (question.type === 'essay') {
     // الأسئلة المقالية تحتاج تصحيح يدوي
     // لكن يمكن إعطاء نقاط جزئية تلقائياً حسب الطول
     
     const answerLength = userAnswer.answer ? userAnswer.answer.length : 0;
     
     if (answerLength > 50) {
       // إجابة طويلة - نقاط كاملة (يحتاج مراجعة يدوية)
       isCorrect = true;
       pointsEarned = question.points || 1;
     } else if (answerLength > 20) {
       // إجابة متوسطة - نصف النقاط
       isCorrect = true;
       pointsEarned = Math.floor((question.points || 1) * 0.5);
     } else {
       // إجابة قصيرة - بدون نقاط (يحتاج مراجعة يدوية)
       isCorrect = false;
       pointsEarned = 0;
     }
     
     // ملاحظة: الأدمن يمكنه تعديل الدرجة يدوياً لاحقاً
   }
   ```

3. **حساب النتائج:**
   ```javascript
   // جمع الدرجات
   totalScore += pointsEarned;
   maxScore += question.points || 1;
   
   // حفظ تفاصيل كل سؤال
   gradedAnswers.push({
     questionId: question.id,
     questionText: question.questionText,
     questionType: question.type,
     answer: userAnswer.answer,           // إجابة الطالب
     correctAnswer: question.correctAnswer, // الإجابة الصحيحة
     isCorrect: isCorrect,                // صحيح/خطأ
     earnedMarks: pointsEarned,            // النقاط المكتسبة
     maxMarks: question.points || 1       // النقاط القصوى
   });
   ```

**ج) حساب النسبة المئوية والتقدير:**

```javascript
// حساب النسبة المئوية
const percentage = maxScore > 0 
  ? Math.round((totalScore / maxScore) * 100) 
  : 0;

// تحديد التقدير
let grade = 'F';
if (percentage >= 97) grade = 'A+';
else if (percentage >= 93) grade = 'A';
else if (percentage >= 90) grade = 'A-';
else if (percentage >= 87) grade = 'B+';
else if (percentage >= 83) grade = 'B';
else if (percentage >= 80) grade = 'B-';
else if (percentage >= 77) grade = 'C+';
else if (percentage >= 73) grade = 'C';
else if (percentage >= 70) grade = 'C-';
else if (percentage >= 67) grade = 'D+';
else if (percentage >= 63) grade = 'D';
else if (percentage >= 60) grade = 'D-';

// تحديد المستوى
let level = 'Poor';
if (percentage >= 95) level = 'Excellent';
else if (percentage >= 85) level = 'Very Good';
else if (percentage >= 75) level = 'Good';
else if (percentage >= 65) level = 'Average';
else if (percentage >= 50) level = 'Below Average';

// التحقق من النجاح
const passingScore = exam.passingScore || 60;
const passed = percentage >= passingScore;
```

**د) حفظ النتيجة:**

```javascript
// إنشاء سجل ExamResult
const examResult = new ExamResult({
  studentId: userId,
  courseId: courseId,
  examId: examId,
  examTitle: exam.title,
  score: totalScore,           // الدرجة المكتسبة
  maxScore: maxScore,          // الدرجة القصوى
  percentage: percentage,      // النسبة المئوية
  grade: grade,                // التقدير (A+, A, B+, إلخ)
  level: level,                // المستوى
  passed: passed,              // نجح/رسب
  answers: gradedAnswers,      // تفاصيل كل إجابة
  submittedAt: new Date(),     // وقت الإرسال
  timeSpent: timeSpent || 0    // الوقت المستغرق
});

await examResult.save();
```

**هـ) إرجاع النتيجة للطالب:**

```javascript
// الاستجابة المرسلة للطالب
{
  success: true,
  result: {
    score: 85,              // الدرجة
    maxScore: 100,          // الدرجة القصوى
    percentage: 85,          // النسبة المئوية
    grade: "B",             // التقدير
    level: "Very Good",      // المستوى
    passed: true,           // نجح
    answers: [              // تفاصيل كل سؤال
      {
        questionId: "q_1234567890_0",
        questionText: "ما هي عاصمة مصر؟",
        questionType: "multiple_choice",
        answer: "opt_q_1234567890_0_1",      // إجابة الطالب
        correctAnswer: "opt_q_1234567890_0_1", // الإجابة الصحيحة
        isCorrect: true,     // ✅ صحيح
        earnedMarks: 2,     // النقاط المكتسبة
        maxMarks: 2         // النقاط القصوى
      },
      {
        questionId: "q_1234567890_1",
        questionText: "مصر تقع في قارة أفريقيا",
        questionType: "true_false",
        answer: false,       // إجابة الطالب (خطأ)
        correctAnswer: true, // الإجابة الصحيحة (صحيح)
        isCorrect: false,    // ❌ خطأ
        earnedMarks: 0,
        maxMarks: 1
      }
      // ... باقي الأسئلة
    ],
    submittedAt: "2025-02-05T10:30:00Z"
  }
}
```

---

### 4️⃣ عرض النتيجة للطالب

#### شاشة النتيجة:

1. **ملخص النتيجة:**
   - ✅ الدرجة: 85/100
   - ✅ النسبة المئوية: 85%
   - ✅ التقدير: B
   - ✅ المستوى: Very Good
   - ✅ الحالة: نجح ✓

2. **تفاصيل الأسئلة:**
   - لكل سؤال:
     - نص السؤال
     - إجابة الطالب
     - الإجابة الصحيحة
     - الحالة: ✓ صحيح / ✗ خطأ
     - النقاط: X/Y

3. **إحصائيات:**
   - عدد الأسئلة الصحيحة: X/Y
   - عدد الأسئلة الخاطئة: X/Y
   - الوقت المستغرق: X دقائق

---

### 5️⃣ الميزات المتقدمة

#### أ) إعادة المحاولة:
- بعض الامتحانات قابلة للتعديل (`isEditable: true`)
- الطالب يمكنه إعادة الإجابة وتحديث النتيجة
- يتم حفظ آخر نتيجة فقط

#### ب) التصحيح اليدوي للأسئلة المقالية:
- الأدمن يمكنه مراجعة الأسئلة المقالية
- تعديل الدرجة يدوياً
- إضافة ملاحظات

#### ج) الإحصائيات:
- حفظ جميع النتائج في `ExamResult` Model
- إمكانية عرض:
  - متوسط الدرجات
  - أعلى/أقل درجة
  - معدل النجاح
  - التقدم الزمني

---

### 6️⃣ أمثلة عملية

#### مثال 1: امتحان بسيط

**الامتحان:**
- العنوان: "امتحان الرياضيات - الفصل الأول"
- عدد الأسئلة: 5
- الدرجة الكلية: 50
- درجة النجاح: 60%

**الأسئلة:**
1. MCQ: "2 + 2 = ?" → الإجابة الصحيحة: "4" (2 نقاط)
2. MCQ: "5 × 3 = ?" → الإجابة الصحيحة: "15" (2 نقاط)
3. True/False: "10 > 5" → الإجابة الصحيحة: `true` (1 نقطة)
4. True/False: "3 < 2" → الإجابة الصحيحة: `false` (1 نقطة)
5. Essay: "اشرح عملية الضرب" → (10 نقاط)

**نتيجة الطالب:**
- الإجابات: [صحيح، صحيح، صحيح، خطأ، إجابة مقالية]
- الدرجة: 2 + 2 + 1 + 0 + 5 = 10/50
- النسبة: 20%
- التقدير: F
- الحالة: رسب

#### مثال 2: امتحان متقدم

**الامتحان:**
- العنوان: "امتحان اللغة العربية - النحو"
- عدد الأسئلة: 20
- الدرجة الكلية: 100
- درجة النجاح: 60%

**النتيجة:**
- الدرجة: 85/100
- النسبة: 85%
- التقدير: B
- المستوى: Very Good
- الحالة: نجح ✓

---

### 7️⃣ ملاحظات مهمة للمطورين

#### أ) هيكل البيانات:
- ✅ استخدام `option.id` للإجابات الصحيحة في MCQ
- ✅ استخدام `boolean` للإجابات في True/False
- ✅ حفظ جميع الإجابات في `ExamResult.answers`

#### ب) الأمان:
- ✅ التحقق من تسجيل الطالب في الدورة
- ✅ منع الوصول للامتحانات غير المسموحة
- ✅ التحقق من صحة البيانات المرسلة

#### ج) الأداء:
- ✅ استخدام Indexes في MongoDB للبحث السريع
- ✅ تخزين النتائج بشكل منفصل عن الامتحانات
- ✅ دعم التصحيح الفوري

---

## 🔐 نظام المصادقة والأمان

### المصادقة:
- **JWT Tokens**: للمصادقة الآمنة
- **Password Hashing**: باستخدام bcryptjs
- **Phone-based Auth**: تسجيل الدخول برقم الهاتف
- **Role-based Access Control**: صلاحيات حسب الدور

### الأمان:
- **Rate Limiting**: حماية من الهجمات
- **CORS**: إعدادات متقدمة
- **Helmet**: حماية من الثغرات الأمنية
- **Input Validation**: التحقق من المدخلات
- **Error Handling**: معالجة أخطاء شاملة

---

## 💳 نظام المدفوعات

### طرق الدفع المدعومة:
1. **Vodafone Cash** (الطريقة الافتراضية)
2. **PayPal**
3. **Stripe**
4. **Bank Transfer** (تحويل بنكي)

### تدفق الدفع:
1. الطالب يختار دورة
2. يرفع إثبات الدفع (Screenshot)
3. الأدمن يراجع الطلب
4. الموافقة/الرفض:
   - **الموافقة**: تفعيل الدورة تلقائياً + إشعار
   - **الرفض**: إشعار مع سبب الرفض

### حالات الدفع:
- `pending` - قيد المراجعة
- `accepted` - تمت الموافقة
- `rejected` - تم الرفض

---

## 📊 نماذج البيانات الرئيسية (Models)

### 1. User Model:
```javascript
{
  firstName, secondName, thirdName, fourthName,  // الاسم الكامل
  phoneNumber,                                    // رقم الهاتف (E.164)
  password,                                       // كلمة المرور (مشفرة)
  role: ['student', 'admin', 'parent'],          // الدور
  studentId,                                      // رقم الطالب (لطلاب فقط)
  grade,                                          // الصف الدراسي
  governorate,                                    // المحافظة
  enrolledCourses: [{                            // الدورات المسجل فيها
    course: ObjectId,
    enrolledAt: Date,
    progress: Number,
    completed: Boolean,
    paymentStatus: ['pending', 'approved', 'rejected']
  }],
  linkedStudents: [ObjectId],                    // الطلاب المرتبطين (لأولياء الأمور)
  isActive: Boolean                              // حالة الحساب
}
```

### 2. Course Model:
```javascript
{
  title,                                          // عنوان الدورة
  description,                                    // الوصف
  grade: ['7', '8', '9', '10', '11', '12'],     // الصف الدراسي
  subject,                                        // المادة
  term: ['Term 1', 'Term 2'],                    // الفصل الدراسي
  price,                                          // السعر
  coverImage,                                     // صورة الغلاف
  videos: [{                                     // الفيديوهات
    title, url, videoId, provider, duration
  }],
  exams: [{                                      // الاختبارات
    id, title, type,
    questions: [{
      questionText, type, options, correctAnswer, points
    }],
    totalMarks, duration, passingScore
  }],
  enrolledStudents: [ObjectId],                  // الطلاب المسجلين
  createdBy: ObjectId                            // منشئ الدورة
}
```

### 3. Payment Model:
```javascript
{
  studentId: ObjectId,                           // الطالب
  courseId: ObjectId,                            // الدورة
  amount: Number,                                // المبلغ
  screenshot: String,                            // إثبات الدفع
  status: ['pending', 'accepted', 'rejected'],  // الحالة
  transactionId: String,                         // رقم المعاملة
  acceptedAt: Date,                              // تاريخ الموافقة
  acceptedBy: ObjectId                          // من وافق
}
```

### 4. ExamResult Model:
```javascript
{
  studentId: ObjectId,                           // الطالب
  courseId: ObjectId,                            // الدورة
  examId: String,                                // معرف الاختبار
  score: Number,                                 // الدرجة
  maxScore: Number,                              // الدرجة الكلية
  percentage: Number,                            // النسبة المئوية
  grade: ['A+', 'A', 'B+', 'B', 'C+', 'C', 'D+', 'D', 'F'],
  answers: [{                                   // الإجابات
    questionId, answer, isCorrect, earnedMarks
  }],
  submittedAt: Date                             // تاريخ الإرسال
}
```

### 5. Group Model:
```javascript
{
  name,                                          // اسم المجموعة
  description,                                   // الوصف
  createdBy: ObjectId,                          // منشئ المجموعة
  students: [ObjectId],                         // الطلاب
  announcements: [{                             // الإعلانات
    title, content, createdAt, isImportant
  }],
  chatMessages: [{                              // الرسائل
    content, sender, timestamp
  }]
}
```

### 6. Notification Model:
```javascript
{
  userId: ObjectId,                              // المستخدم
  type: String,                                  // نوع الإشعار
  title: String,                                 // العنوان
  message: String,                               // الرسالة
  read: Boolean,                                 // مقروء/غير مقروء
  priority: ['low', 'medium', 'high', 'urgent'],
  category: ['payment', 'course', 'system']
}
```

---

## 🛣️ API Endpoints الرئيسية

### Authentication:
- `POST /api/auth/register` - التسجيل
- `POST /api/auth/login` - تسجيل الدخول
- `GET /api/auth/me` - بيانات المستخدم الحالي
- `PUT /api/auth/update` - تحديث الملف الشخصي

### Courses:
- `GET /api/courses` - عرض جميع الدورات
- `GET /api/courses/:id` - تفاصيل دورة
- `POST /api/courses` - إنشاء دورة (Admin)
- `PUT /api/courses/:id` - تحديث دورة (Admin)
- `DELETE /api/courses/:id` - حذف دورة (Admin)
- `POST /api/courses/:id/enroll` - التسجيل في دورة

### Payments:
- `POST /api/payment/upload-proof` - رفع إثبات الدفع
- `GET /api/payment/my-payments` - مدفوعاتي
- `GET /api/admin/payments` - جميع المدفوعات (Admin)
- `PUT /api/admin/payments/:id/approve` - الموافقة (Admin)
- `PUT /api/admin/payments/:id/reject` - الرفض (Admin)

### Exams:
- `GET /api/exams/:courseId/:examId` - الحصول على اختبار
- `POST /api/exams/:courseId/:examId/submit` - إرسال إجابات
- `GET /api/exams/results` - نتائجي
- `GET /api/exams/results/:id` - تفاصيل نتيجة

### Groups:
- `GET /api/groups` - مجموعاتي
- `GET /api/groups/:id` - تفاصيل مجموعة
- `POST /api/admin/groups` - إنشاء مجموعة (Admin)
- `POST /api/admin/groups/:id/announcement` - إضافة إعلان (Admin)

### Notifications:
- `GET /api/notifications` - إشعاراتي
- `PUT /api/notifications/:id/read` - تحديد كمقروء
- `PUT /api/notifications/read-all` - تحديد الكل كمقروء

---

## 🔄 تدفقات العمل الرئيسية

### 1. تدفق التسجيل في دورة:
```
طالب → اختيار دورة → رفع إثبات دفع → انتظار موافقة → تفعيل الدورة
```

### 2. تدفق أداء اختبار:
```
طالب → اختيار اختبار → الإجابة على الأسئلة → إرسال → تصحيح تلقائي → عرض النتيجة
```

### 3. تدفق الموافقة على دفع:
```
أدمن → مراجعة طلب دفع → الموافقة → تفعيل الدورة تلقائياً → إشعار للطالب
```

### 4. تدفق ربط ولي أمر بطالب:
```
ولي أمر → طلب ربط → أدمن يراجع → الموافقة → ربط الحسابات
```

---

## 📱 الميزات الإضافية

### 1. نظام الإشعارات:
- إشعارات فورية عبر Socket.io
- إشعارات في الواجهة
- إشعارات تلقائية للأحداث المهمة

### 2. نظام المجموعات:
- مجموعات دراسية
- إعلانات مهمة
- محادثات جماعية
- مشاركة ملفات

### 3. نظام التقدم:
- تتبع مشاهدة الفيديوهات
- حساب نسبة الإنجاز
- إحصائيات مفصلة

### 4. نظام البحث:
- بحث في الدورات
- بحث في الطلاب (لأدمن)
- فلترة متقدمة

---

## 🎨 الواجهة والتصميم

### المميزات:
- تصميم عصري وجذاب
- دعم الوضع الفاتح والداكن (Dark Mode)
- متجاوب مع جميع الأجهزة (Responsive)
- واجهة عربية كاملة
- رسوم متحركة سلسة (Framer Motion)

### المكونات الرئيسية:
- LuxuryCard, LuxuryButton - مكونات فاخرة
- AdminLayout - تخطيط لوحة الأدمن
- ProtectedRoute - حماية المسارات
- Navigation - شريط التنقل

---

## 🚀 النشر والتشغيل

### متطلبات التشغيل:
- Node.js >= 14.0.0
- MongoDB Atlas
- PM2 (للإنتاج)

### خطوات التشغيل:
1. تثبيت التبعيات: `npm install`
2. إعداد ملف `.env`
3. بناء Frontend: `cd client && npm run build`
4. تشغيل الخادم: `npm start` أو `pm2 start`

---

## 📝 ملاحظات مهمة للمطورين

### 1. هيكل الملفات:
- **Controllers**: معالجة المنطق
- **Models**: تعريفات قاعدة البيانات
- **Routes**: تعريفات المسارات
- **Middleware**: معالجات مخصصة
- **Utils**: أدوات مساعدة

### 2. أفضل الممارسات:
- استخدام async/await
- معالجة الأخطاء بشكل شامل
- التحقق من المدخلات
- استخدام Indexes في MongoDB
- تحسين الأداء (Compression, Caching)

### 3. الأمان:
- تشفير كلمات المرور
- التحقق من الصلاحيات
- Rate Limiting
- Input Sanitization

### 4. الأداء:
- Lazy Loading للمكونات
- Compression للاستجابات
- Indexing في قاعدة البيانات
- Caching عند الحاجة

---

## 📞 الدعم والمساعدة

للمساعدة التقنية أو الاستفسارات:
- راجع ملفات README في كل مجلد
- راجع ملفات التعليقات في الكود
- تواصل مع فريق التطوير

---

**آخر تحديث**: فبراير 2025  
**الإصدار**: 1.0.0
